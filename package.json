{
  "name": "pps-retail-inventory-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-store retail inventory portal (NestJS API + Angular web, single Railway service).",
  "scripts": {
    "setup": "npm --prefix api install && npm --prefix web install",
    "dev:db": "docker compose up -d",
    "dev:api": "npm --prefix api run start:dev",
    "dev:web": "npm --prefix web start",
    "build": "npm run build:web && npm run copy:client && npm run build:api",
    "build:web": "npm --prefix web run build",
    "build:api": "npm --prefix api run build",
    "copy:client": "node scripts/copy-client.mjs",
    "db:generate": "npm --prefix api run db:generate",
    "db:migrate": "npm --prefix api run db:migrate",
    "db:seed": "npm --prefix api run db:seed",
    "start": "npm --prefix api run start:prod"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
